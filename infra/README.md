# Infraestructura y Datos
Scripts para levantar el entorno.

## init.sql
Este script corre automáticamente la primera vez que se levanta el contenedor de Postgres.
Define:
- Tablas base (Firms, Clients, Cases).
- Extensión `vector` para IA.
